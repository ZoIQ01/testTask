services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - SECRET_KEY=django-insecure-docker-dev-key-please-change-me
      - DEBUG=True
      - ALLOWED_HOSTS=localhost,127.0.0.1
    restart: unless-stopped
